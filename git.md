git branch es para crear una nueva rama
git checkout es para cambiar de un commit o de una rama
git tag es para crear etiquetas 
git init para crear un nuevo repositorio
git merge para juntar 
git commit para guardar progreso
